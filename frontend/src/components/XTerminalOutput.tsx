interface XTerminalOutputProps {
  content: string;
  createdDate: Date;
  terminalPath: string;
}

const XTerminalOutput = ({ content, createdDate, terminalPath }: XTerminalOutputProps) => {
  return <div>XTerminalOutput</div>;
};

export default XTerminalOutput;
